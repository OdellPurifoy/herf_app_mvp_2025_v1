---
scope: Whole-system architecture spine for herf_app_mvp_2025_v1, derived from the existing Rails brownfield codebase
purpose: build-substrate
altitude: initiative
updated: 2026-09-15T22:01
---

- (decision by user) New cross-cutting side effects (notifications, billing) move to an explicit service/orchestrator layer called from controllers; AR callbacks stay reserved for internal invariants only (e.g. token generation, timestamps) — prevents new hidden callback chains as the app grows
- (decision by user) Plan-limit enforcement (membership/event/special-offer caps) centralizes into one PlanLimitPolicy object all three models call, replacing the three near-duplicate validation methods — prevents a 4th inconsistent copy
- (decision by user) One lounge per LoungeOwner is the fixed invariant going forward, even though the schema allows many — code may rely on lounge_owner.lounges.first; true multi-lounge is a deliberate future migration, not an assumption new features must preserve
- (decision by user) Production deployment platform is Railway (Docker-based); SIDEKIQ_SETUP.md's Heroku instructions are stale and should be corrected/removed
- (decision by user) New authorized resources use a Pundit policy layer instead of ad hoc per-controller checks — makes missing checks visible/testable (the admin-auth gap was a silent ad hoc omission)
- (version) Pundit 2.5.2 verified current on rubygems.org (Sep 2025), framework-agnostic plain-Ruby authorization, compatible with Rails 7.1
- (decision by user) [ADOPTED] Paradigm is Rails MVC with a thin service/policy layer emerging: fat-model conventions stay for record-local invariants; cross-cutting concerns (notifications, plan limits, authorization) move to dedicated objects per the decisions above
- (decision by user) [ADOPTED] LoungeOwner and Admin are permanently separate Devise models, never merged — platform operator identity must never mix with paying-customer identity
- (decision by user) [ADOPTED] Memberships (patrons) never get login credentials; all member-facing actions (RSVP) are authorized solely by possession of a secure random token (Rsvp#rsvp_token), never a session
- (decision by user) [ADOPTED] pay_subscriptions/pay_customers (Pay gem) are the single source of truth for subscription state; lounge_owner.subscribed? always reads live from Pay tables — no cached/duplicated subscription-status field elsewhere
- (decision by user) [ADOPTED] All member communication (email and SMS) is enqueued via ActiveJob (deliver_later / *Job.perform_later) onto Sidekiq — never delivered synchronously in the request cycle
- (constraint) No CI pipeline exists; nothing currently gates merges (rspec/RuboCop run locally only) — noted in AGENTS.md, not re-decided here
- (event) Reviewer Gate ran: version-verification lens (PASS WITH CONCERNS) + adversarial-incompatibility lens (PASS WITH CONCERNS), both as parallel subagents; full reports in reviews/
- (decision) Applied all reviewer fixes at Finalize: corrected Stack table to exact Gemfile.lock versions (Sidekiq 8.0.9 not 7.x; Pay 10.1.5, Devise 4.9.4, twilio-ruby 7.6.1, Honeybadger 6.5.2 flagged as pinned-behind-upstream); tightened AD-1 (single creation entrypoint per entity), AD-2 (PlanLimitPolicy owns counting + period-type + single invocation layer), AD-3 (canonical accessor + unique constraint), AD-4 (policies scoped per identity type), AD-7 (bans derived shadow-authority fields too), AD-8 (broadened to all outbound comms, not just member-facing); added Jobs->Models dependency edge; added token-issuance convention row; added sendgrid-actionmailer unmaintained-gem risk to Deferred
- (question) Adversarial reviewer also flagged AD-5 not covering a hypothetical future third identity type (staff/sub-accounts) — declined: no such feature exists or is planned, fails the real-trade-off test, not added to Deferred since it isn't a known gap being pushed down
- (event) spine finalized
