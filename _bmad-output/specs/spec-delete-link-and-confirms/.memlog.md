---
topic: Broken edit-page Delete Event link (backlog bug #3) and non-functional confirmation dialogs app-wide
updated: 2026-09-16T17:16
---

- (note) Source: backlog bug #3 'canceling an event routes back to the event show page which is gone, reroute to dashboard'; root cause verified in code on main dc073e1
- (decision) Root cause: app/views/events/edit.html.erb 'Delete Event' is link_to with rails-ujs method: :delete, but rails-ujs is not installed (config/importmap.rb pins only Turbo/Stimulus), so the click is a plain GET /events/:id; no events/show view exists, so the owner hits an error page and nothing is deleted. It is the only UJS-style method link in app/views
- (decision) Related root cause: 41 data: { confirm: } attributes across app/views are rails-ujs syntax with no handler (no JS in app/javascript references confirm), so no confirmation dialog appears anywhere; destructive deletes fire on first click. Only event_registrations/show.html.erb uses working data: { turbo_confirm: }
- (direction by user) Scope: fix the edit-page link AND convert all 41 confirms (including sign-out), preserving each as originally written
- (capability) CAP-1: Delete Event from edit page — intent: a lounge owner can delete an event from its edit page and lands on the dashboard; success: after confirming, the click sends DELETE /events/:id, the event is removed, the owner is redirected to the dashboard with the destroyed notice, and no GET to /events/:id occurs
- (capability) CAP-2: Working confirmation prompts — intent: every action that declares a confirmation message prompts before proceeding; success: for each of the 41 prompts, dismissing the dialog sends no request and accepting proceeds with the original action
- (constraint) Use Turbo's native data-turbo-method / data-turbo-confirm; do not reinstall rails-ujs or add a JS dependency — Hotwire via importmap is the established stack
- (constraint) Keep every confirm message's text verbatim and keep the same set of confirmed actions — this restores intended behavior, it does not redesign it
- (constraint) Event deletion must still go through EventsController#destroy so its dashboard redirect and the Event after_destroy cancellation notifications (SMS + email jobs) still fire
- (note) Non-goals: no events show page; no new confirmations or wording changes; no rails-ujs
- (note) Supersedes spec-rsvp-notification-fixes' claim that bug #3 was already fixed — that check only covered EventsController#destroy's redirect, not the edit-page link
- (event) Self-validate pass 1 (coherence): PASS — both capabilities have intent and testable success, intents are WHAT not HOW, all 3 constraints rule options out, 3 non-goals, success signal is demonstrable in a browser, CAP IDs stable
- (event) Self-validate pass 2 (preservation): PASS — backlog symptom, verified root cause, confirm finding, and user scope decision all landed; the backlog's 'reroute to dashboard' is already satisfied by EventsController#destroy and is kept as a constraint
- (event) spec finalized
- (decision by user) Story Breakdown: spec_checkpoint true and done_checkpoint true on both stories; no invoke_dev_with notes
- (decision) Story 1 (id 1): Fix the edit-page Delete Event link, maps to CAP-1
- (decision) Story 2 (id 2): Restore confirmation prompts app-wide via turbo_confirm, maps to CAP-2
- (event) stories.yaml validity check: PASS — 2 entries, required fields present, quoted string ids '1'/'2' unique and prefix-free, no status field
