---
topic: RSVP re-edit lockout and missing event-cancellation email, from the Notion backlog
updated: 2026-09-16T17:14
---

- (note) Source: Notion backlog items pasted by user, 3 items about RSVP/event-cancellation flow; 2 of 3 root-caused directly in code, 1 unreproducible in current code
- (capability) CAP-1: RSVP re-edit after first response — intent: a member who already responded (attending/declined) can still change their response via the same link any time before the RSVP's real expires_at, not just on their first visit; success: visiting an unexpired RSVP link after a prior response renders the editable form, not the expired page, and a changed submission updates status/guest_count
- (decision) Root cause of CAP-1: RsvpsController#update guards on Rsvp#valid_for_response? (requires status == pending), so any already-answered-but-unexpired RSVP is rejected with the same message as a truly expired one — the fix loosens the re-edit gate to expired? alone, not valid_for_response?
- (capability) CAP-2: Event cancellation email — intent: when a lounge owner deletes an event, every active member with email notifications enabled receives an Event Cancelled email (existing CancelledEventMailer), mirroring the SMS notice that already sends; success: deleting an event results in CancelledEventMailer#notify being delivered to each eligible member, verified by job/mailer spec
- (decision) Root cause of CAP-2: CancelledEventMailer exists (has its own spec + preview) but has zero call sites in application code; EventDeletionNotificationJob only sends SMS via SmsNotificationService and never invokes the mailer — it was simply never wired up
- (constraint) Reuse the existing CancelledEventMailer/view as-is — it's already built and tested; no new template needed
- (constraint) CAP-1's fix must preserve the expired? guard exactly as-is — an RSVP past its real expires_at must still show the expired page; only the already-responded gate loosens
- (constraint) Email eligibility for CAP-2 mirrors the SMS eligibility pattern already in EventDeletionNotificationJob (active membership, notification-preference opt-in) but for allow_email_notifications instead of allow_text_notifications
- (assumption) Implementer decides, per ARCHITECTURE-SPINE.md AD-1, whether to extend the existing EventDeletionNotificationJob (matches current pattern) or extract an explicit service — the spec fixes WHAT (email must send) not HOW; adopted as a companion for that context
- (note) Third backlog item (post-cancel redirect to gone event page) is not reproducible: EventsController#destroy already redirects to dashboard_path for both html and turbo_stream formats. User confirmed: treat as already fixed, excluded from this spec's capabilities
- (event) Self-validate pass 1 (coherence, rules 1-6+8): PASS — both capabilities have intent+success, intents are WHAT not HOW, all 3 constraints rule something out, 3 non-goals present, success signal is testable, CAP IDs stable, prose lean
- (event) Self-validate pass 2 (preservation): PASS — all 3 source backlog claims accounted for (2 as capabilities, 1 as non-goal + open question); no wrapper-only content dropped
- (event) spec finalized
- (decision) Story Breakdown: user wants human review of each story's implementation spec before coding starts (spec_checkpoint: true for all), and a pause after each story completes before the next starts (done_checkpoint: true for all); no invoke_dev_with notes needed for either story
- (decision) Story 1 (id 1): Fix RSVP re-edit lockout after first response, maps to CAP-1
- (decision) Story 2 (id 2): Send event-cancellation email on delete, maps to CAP-2
- (event) stories.yaml validity check: PASS — 2 entries, all required fields present, ids '1'/'2' unique and prefix-free, quoted strings, no status field
- (decision) Correction: third backlog item (cancel routes to a gone event page) is NOT fixed. Earlier check only covered EventsController#destroy's redirect; the real cause is the edit page's rails-ujs 'Delete Event' link issuing a GET. Moved to its own spec, spec-delete-link-and-confirms; open question resolved
